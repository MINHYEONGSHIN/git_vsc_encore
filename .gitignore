secreats.yaml

# 주석
# secreats.yaml
#*.yaml
# logs (logs 이름을 가지는 파일 또는 폴더와 그 내용들을 관리로 맡기지 않겠다)
# logs/debug.log (logs 폴더 밑에 있는 debug.log)
# logs/*.log
# logs/**/debug.log(logs 폴더 밑에 있는 폴더와 파일을 포함해서 debug.log 제외)
